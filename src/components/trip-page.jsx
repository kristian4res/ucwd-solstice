import React from 'react'
import PageContainer from './page-container';

const TripPage = ({ tripDetails }) => {
  return (
    <PageContainer>
        <section className='min-h-screen'>
                
        </section>
    </PageContainer>
  )
}

export default TripPage;